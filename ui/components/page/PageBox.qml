import QtQuick
import QtQuick.Controls
import QtQuick.Layouts

// * The PageBox class manages the box object in page.
Rectangle
{
    color: "#162A35"
    radius: 10
    border.color: Material.color(Material.Grey)
    border.width: 2
}