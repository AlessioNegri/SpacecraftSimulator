import QtQuick
import QtQuick.Controls
import QtQuick.Layouts

// * The SectionItemName class manages the generic parameter name.
Text
{
    color: "#FFFFFF"
    font.pointSize: 12
    font.bold: true
}